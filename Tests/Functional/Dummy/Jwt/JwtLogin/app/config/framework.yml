framework:
    secret: test
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
    validation: { enabled: true, enable_annotations: true } # omittable?
    assets: ~ # omittable?
    csrf_protection: true # omittable?
    form: true # omittable?
    test: ~
    default_locale: en #omittable?
    session:
        storage_id: session.storage.mock_file
    profiler: { only_exceptions: false } # omittable?
